name: your_app_name
description: A new Flutter project

publish_to: 'none' # Remove this line if you plan to publish

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.14.0
  firebase_auth: ^4.6.3
  cloud_firestore: ^4.7.3
  # Add more Firebase services as needed, e.g.:
  # firebase_database: ^10.1.0
  # firebase_messaging: ^14.3.0

  # Other dependencies
  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
